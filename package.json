{
  "name": "blog-twitter-purescript",
  "version": "1.0.0",
  "description": "Companion code to blog article",
  "main": "index.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "build": "PATH=node_modules/.bin:$PATH pulp build",
    "start": "PATH=node_modules/.bin:$PATH pulp run",
    "test": "PATH=node_modules/.bin:$PATH pulp test",
    "bundle": "PATH=node_modules/.bin:$PATH pulp bundle"
  },
  "author": "Rajiv Abraham",
  "license": "ISC",
  "dependencies": {
    "node-fetch": "^2.2.0",
    "text-encoding": "^0.7.0"
  },
  "devDependencies": {
    "pulp": "^12.3.0",
    "purescript": "^0.12.0"
  }
}
